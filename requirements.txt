fastapi==0.111.0
uvicorn[standard]==0.30.1
# pydantic version must satisfy both FastAPI (<3.0, >=1.7.4) and supabase/realtime (>=2.11.7).
# Use a range so that Vercel can install a pydantic/pydantic-core combo that supports
# the Python version in its build image (currently 3.14).
pydantic>=2.11.8,<3.0.0
supabase==2.18.1
python-dotenv==1.0.0
mangum==0.17.0
